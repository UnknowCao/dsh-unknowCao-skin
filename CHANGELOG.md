# Changelog

本插件的每一次风格改动都记在这里。皮肤换过三轮风格，包名一直没跟着改（`dsh-paper-skin` 起于第一轮的宣纸皮肤），
原因见每条条目。

## 0.5.0 — 彩度纪律

上一版的问题不是配色难看，是彩度失控：表面色阶用到 28%–55% 的饱和度，深色模式整片变成饱和钢蓝
（浮层是 `#476791`），悬停蒙层还染着品牌蓝——颜色到处都在说话，就没有一处显得贵。

- 表面色阶彩度压到 6%–15%，色相仍偏冷但近乎中性
- 深色底色由 `#0B1623` 压到 `#0C1119`（近黑），层级改由明度拉开而不是色相
- 悬停蒙层改回中性墨色：悬停是「按下去一点」，不是「蓝一下」
- 主按钮蓝由 `#1560C9` 收到 `#1B5FC1`
- 天光透明度由 5%–11% 收到 5%–7%

审计过程中抓到一处自查回归：`bluish-600`（深色下的 caption 色）被顺手调暗，深色 caption 从 4.92:1 掉到
4.10:1、跌穿 AA，已按产品原亮度修正。反过来，浮层压暗修好了产品自带的一个窟窿——深色浮层里的次要文字
从产品的 3.85:1 变成 6.97:1。

## 0.4.0 — 蔚来 · 地平线

整体风格换成蔚来气质，取自其公开的品牌语言「Blue Sky Coming」（标识上半是天、下半是地，天地交汇处是
地平线）。色值是按这份语言自己配的，不是官方色卡；图形自画，不含官方资源。

- 浅色近白 / 深色座舱底；主按钮改成天蓝（这套皮肤唯一的强调色）
- 字体从衬线 + 楷体回到几何无衬线（蔚来的身份在色与留白，不在字）
- 质感层由纸纹换成顶部天光
- 品牌标记由朱砂方印换成「天—地平线—地」渐变方印

## 0.3.0–0.3.1 — 品牌层

新增品牌层：`sidebar.brand.name`、`sidebar.brand.mark`、`conversation.hero.brand.mark` 三个 `single` 槽，
外加浏览器标签页标题（外壳 HTML 里写死了 `<title>DeepSeek Harness</title>`，前端 bundle 从不重写它，
所以只能由皮肤来改）。全部记下原值，卸载逐字节还原。

品牌名在这个区间内调整过几次，最终定为 `UnknowCao Harness`；标签页标题取 `BRAND.name`，两处是同一个真源。

## 0.2.0–0.2.1 — 从「暖白」到真正的皮肤

v0.1 只改了主题服务公布的 13 个别名 token，结果是页面从冷白变成暖白，而其余九成表面仍是产品的冷灰蓝。
这一版铺到设计系统自己的变量层：**产品的 73 个静态色阶与 21 个写死字面值的别名全部重绑**。

同时补上了 `test/palette-audit.mjs`：在 stub 浏览器里真实加载 `client.js`，对账覆盖率 / 漂移 / 对比度。
它当场抓到两个真缺陷：一是换色相时守住的是 HSL 明度而非 WCAG 亮度（暖色在同一 HSL 明度下实际更亮，
浅色三级文字从 3.71 掉到 2.72），二是凭空发明了两个产品里不存在的色阶步。

## 0.1.0 — 宣纸

初版。只做 13 个别名 token 的覆盖，风格是宣纸底 + 墨字 + 朱砂。包装名 `dsh-paper-skin` 从这里来。
