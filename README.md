# dsh-paper-skin · 蔚来 · 地平线

> DSH Web GUI 皮肤：**近白底、近黑座舱、一支天蓝点睛**，界面上显示的 DeepSeek 品牌字样已换成自己的。装上即生效，卸载即还原。

```
近白 #F7F9FC    近黑 #080C12    天蓝 #1B5FC1    座舱底 #0C1119
```

> **注意：包名与目录仍是 `dsh-paper-skin`（最早是宣纸皮肤）。** 皮肤内容换了两轮，名字一直没跟着改——改名要动 profile 里的依赖和 bundles 名单，我没擅自动。想改说一声，一条命令的事。

---

## 概念

取自蔚来公开的品牌语言：**「Blue Sky Coming」**——标识上半是天、下半是地，天地交汇处是地平线。这套皮肤把那份语言翻译成界面：浅色是受光的天，深色是夜里的座舱，强调色是中间那道天蓝。

**色值是我按这份语言自己配的，不是蔚来的官方标准色卡。** 蔚来没有公开可核实的标准色值（能查到的第三方色卡站要么只给了 `Black`，要么被 Cloudflare 挡着），所以这里不声称复刻。图形也是自己画的抽象「天—地平线—地」，**不含任何蔚来官方资源**。

### 彩度纪律

初版的毛病不是配色难看，是**彩度失控**：表面色阶用了 28%–55% 的饱和度，深色模式整片变成饱和钢蓝（浮层是 `#476791` 那种蓝），悬停蒙层还染着品牌蓝——颜色到处都在说话，就没有一处显得贵。

这一版把说话权收回到一处：

- **表面色阶近乎中性**（色相仍偏冷，彩度压到 6%–15%）
- **全屏只有一个饱和的东西**，就是强调族那一支天蓝
- **深色模式压成近黑**（`#0C1119`，不是 `#0B1623` 那种蓝黑），层级靠明度拉开而不是靠色相
- **悬停蒙层改回中性墨色**——悬停是「按下去一点」，不是「蓝一下」

顺带把产品自带的一个对比度窟窿补上了：深色浮层里的次要文字从产品的 3.85:1 变成 **6.97:1**。

---

## 效果

| | 浅色 · 天光 | 深色 · 座舱 |
|---|---|---|
| 页面底 `bg-base` | `#F7F9FC` | `#0C1119` |
| 卡片 / 嵌套面 `bg-layer-1/2` | `#F7F9FC` | `#18202F` / `#1E283A` |
| 浮层 `bg-overlay` | `#E2E8F1` | `#333D4E` |
| 侧栏 `specific-sidebar-fill` | `#F2F5F9` | `#121927` |
| 发丝线 `border-l1` | `#0B1B2E14` 冷墨 8% | `#E8EFFA14` 冷白 8% |
| 分隔线 `border-l2` | `#0B1B2E24` | `#E8EFFA24` |
| 品牌 / 主按钮 `brand-primary` | `#1B5FC1` **天蓝** | `#F2F5F9` |
| 正文 `label-primary` | `#080C12` | `#F2F5F9` |
| 次要文字 `label-secondary` | `#4E5D73` | `#C6CFDD` |
| 错误 `state-error` | `#C43A45` | `#D57B80` |
| 成功 `state-success` | `#15784A` | `#7CC6A2` |
| 警告 `state-warn` | `#916200` | `#DCB47C` |
| 天蓝族 `deepseek-500` | `#3B76C8` | 同族 |

**排版**：几何无衬线（Segoe UI Variable / 系统栈），标题回到同一族。蔚来的身份在色与留白，不在字——所以这一版不发明字体。
**天光**：视口顶部一层极淡的天光，向下三分之一处化开；深色下换成座舱氛围灯。5% / 7% 的不透明度，只给平面一个光源方向。
**品牌**：侧栏换成一块「天—地平线—地」的方印 + `UnknowCao Harness`；浏览器标签页标题同理。

### 对比度（实跑，非估算）

`node test/palette-audit.mjs` 对 21 组前景/背景算 WCAG 比值，并与产品自带配色逐组对账。当前：**0 失败**。

| 组 | 浅色 | 深色 | 产品原值 |
|---|---|---|---|
| 正文 / 页面 | 18.58:1 | 17.30:1 | 18.9 / 17.5 |
| 次要文字 / 页面 | 5.43:1 | 12.1:1 | 5.80 / 12.1 |
| 次要文字 / 浮层 | 5.43:1 | **6.97:1** | 4.90 / **3.85** |
| 主按钮文字 / 按钮 | **5.77:1**（蓝底近白字） | 17.92:1 | 18.90 / 18.08 |
| 错误 / 成功 / 警告 | 4.93 / 5.22 / 5.04 | 6.28 / 9.42 / 9.79 | 4.50 / **2.28** / **2.15** |

两处 ⚠ 都是产品本来就低于 AA 的（浅色三级文字 3.64，产品 3.71；浅色 caption 2.23，产品 2.13），偏差在 0.1 以内。**皮肤没有把任何一项从合格拖到不合格**，还顺手修好了三处：深色浮层的正文与次要文字、以及状态色从 2.2:1 拉到 4.9:1 以上。

---

## 四层机制

产品把颜色分成两段：一族 `--dsw-static-*` **静态色阶**，再由 `--dsw-alias-*` **按语义引用**它们。皮肤顺着这个结构分三层，另加一层品牌。

**① 契约层** — `theme.overrideTokens()`，13 个已公布别名。值以 inline style 写在 body 上，优先级高过任何样式表。

**② 系统层** — `--dsw-static-*` 色阶、写死的别名、排版变量。色阶扛下 90% 的表面：**产品的 73 个静态变量和 21 个写死字面值的别名全部被重绑**（审计逐条核对）。

色阶是同一步在明暗两种模式下兼用的单一值——同一步在浅色下当背景、在深色下常常当文字。所以换色只换色相与饱和度，**守住 WCAG 亮度 Y**，不碰明度。这条规矩是拿代价换来的：上一版在恒定 HSL 明度下旋转色相，以为保住了明度就保住了对比度，结果浅色三级文字从 3.71 掉到 2.72——HSL 明度不是感知亮度，把冷灰转成偏蓝的色，同一 HSL 明度下实际更亮。

唯一例外是最上面几步：产品浅色表面是纯白（Y=1.0），而任何带色相的白都到不了 1.0，所以顶部做一次单调压缩、在 Y=0.75 处归零——天光白只从那里来。

**③ 天光层** — `body::after` 铺满视口的渐变光，`pointer-events:none`，产品没占用这个位置。

**④ 品牌层** — 四个可写入口，全部可还原：`sidebar.brand.name`、`sidebar.brand.mark`、`conversation.hero.brand.mark` 三个 `single` 槽注册即遮蔽产品自带的占用者；标签页标题走 `document.title`——外壳 HTML 里写死了 `<title>DeepSeek Harness</title>`，而前端 bundle 从不改它，所以它是 GUI 上唯一一处真正写死的品牌文字。

### 边界收在哪

- 不写任何产品类名选择器，不搬 DOM，不改布局。
- 变量声明在 `body` 上（自定义属性按元素就近取值，声明到 `:root` 会被 body 自己的值遮住）；用 `html body` 拿优先级，不依赖样式表插入顺序，也不用 `!important`。
- 唯一触碰的 DOM 是一个 `<style>` 元素，归当前 Cordis fiber 所有。

---

## 审计

```sh
node test/palette-audit.mjs [path/to/dsh-client-ui-theme/lib/client.js]
```

在 stub 出来的浏览器里真实加载 `client.js`，抓出它实际安装的样式表、token 表和 slot 注册，再和产品的 token 样式表对账。六项检查：**loadable**（bundle id / style 元素 / fiber effect / 声明的依赖）、**brand**（三个槽、品牌名、标记的 `size` 与宿主 `className`、标签页标题）、**colour sanity**、**coverage**（73 个静态色阶 + 21 个写死别名是否全覆盖）、**drift**（13 个 inline token 与色阶推导值是否一致，偏离必须在白名单里写明理由，**新漂移直接失败**）、**contrast**（21 组，同时算产品原值，**跨越 AA 线才算回归**）。

第 4 项是真正的防回归网：系统层依赖 103 个产品内部变量名，上游改名会静默失效，覆盖检查把静默失效变成响亮的失败。

---

## 安装

```sh
dsh plugin --profile web add link:C:/Users/MAC/Desktop/DSH/dsh-paper-skin
```

> **pnpm 12 注意**：`pnpm add <本地路径>` 会报 `ERR_PNPM_PACKAGE_MANAGER_ADD_RESOLVE_LATEST`（`file:` / `link:` / 绝对 / 相对路径全失败）。可靠做法是在 profile 的 `package.json` 写 `"dsh-paper-skin": "link:<绝对路径>"`，再跑 `dsh plugin --profile web install`——`install` 会被转发给 pnpm，并照样自动 reconcile `dsh.profile.bundles`。

新 bundle 的 patch 在启动时合成，**装完要重启一次 profile**（GUI：鲸湾 → 洄游）。

### 卸载

```sh
dsh plugin --profile web remove dsh-paper-skin
```

重启后逐字节回到原配色。

---

## 想调

**注意有两处来源，改一处不够。** 13 个门面 token（`ALIAS_TOKENS`）以 inline 写入，优先级高于样式表——只改 `SKIN_CSS` 里的色阶，这 13 个不会跟着变。约定是：**色阶是基准，`ALIAS_TOKENS` 照抄色阶的推导值**，只有白名单里的五档例外。改完跑一次审计，漂移会被逐条列出来。

- **整体更冷 / 更浓**：这是这套皮肤最值得调的旋钮，也是「优雅」的出处。`--dsw-static-neutral-bluish-*` 那一族现在彩度在 6%–15%，把它整体调高就会滑回饱和钢蓝，调低则趋近纯灰。**别只调深色端**——色阶是明暗共用的。
- **蓝再深 / 再亮一点**：`--dsw-static-deepseek-500` 与 `--dsw-static-blue-500` 一起动（同一族的两个名字）；主按钮的深浅另由 `ALIAS_TOKENS` 里的 `brand-primary` 决定。
- **浅色底再冷一点**：动 `--dsw-static-neutral-bluish-00`（近白的色相）与 `-50`（侧栏），两者一起挪。挪太多先跌破的是三级文字。
- **深色想更「座舱」**：`--dsw-static-neutral-bluish-950`（页面底）往下压，`-875` / `-850`（卡面）跟着压；别动 `-50`——深色下它是正文色。**`-600` 也别压暗**：它是深色下的 caption 色，压暗一点就从 4.9:1 掉到 4.1:1（这条是踩过的）。
- **天光想关掉**：删掉 `html body::after` 那两块，视觉上只剩纯色。
- **改品牌**：`BRAND = { name: 'UnknowCao Harness', seal: 'U' }`，标记渐变在 `MARK_SKY`。审计里的 `EXPECTED_BRAND` 要同步，否则立刻报失败。
- **不换品牌**：`BRAND = null`。

---

## 已知边界

- **首屏会闪一下原配色。** 产品的插件前内联引导只认内置 ui-theme 设置，第三方皮肤挤不进去；`immediately: true` 只能把模块预取提前，消不掉那一帧。
- **两个皮肤插件之间没有仲裁。** 契约层按 `source` 命名可叠加；系统层是纯 CSS，只按优先级和加载顺序取胜。
- **系统层绑在产品内部变量名上。** 上游改名不报错，只会静默失效；审计的覆盖检查是唯一的报警器，得有人跑。
- **名字与内容已经不符。** 包名叫 `dsh-paper-skin`，内容是蔚来气质。改名要同时动 profile 依赖与 `dsh.profile.bundles`。
- **蔚来是别人的品牌。** 这套皮肤只是拿公开的品牌语言做个人化风格，不含任何官方图形资源，与蔚来无关联、也未经其认可，色值不是官方标准色。要分发前请先想清楚这一点。
- **只管 Web GUI。**

---

## 结构

```
dsh-paper-skin/
├── package.json            dsh.bundle.patch + dsh.client.platform: web
├── cordis.patch.yml        往 profile 里插一行（包根）
├── index.js                宿主半：一行启动日志，刻意不做别的
├── client.js               浏览器半：ALIAS_TOKENS（契约层）+ SKIN_CSS（系统层 + 天光层）+ 品牌层
└── test/palette-audit.mjs  审计：加载真 bundle，对账覆盖率 / 漂移 / 对比度 / 品牌槽
```

宿主半存在的唯一原因是**浏览器半的发现方式**：client-modules 顺着"已挂载的行"回溯到包的 `package.json`，读到 `dsh.client.platform: web` 才把 `exports["./client"]` 服务到 `/plugins/dsh-paper-skin/client.js`。一行同时买到两个面。

## 许可

MIT
